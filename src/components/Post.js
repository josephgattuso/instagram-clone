import React from "react";

const Post = () => {
  return (
    <div>
      {/* header > avatar + username */}
      {/* image */}
      {/* username + caption */}
    </div>
  );
};

export default Post;
